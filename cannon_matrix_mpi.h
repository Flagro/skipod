#pragma once

#include "matrix.h"
#include "types.h"

time_type cannon_matrix_multiplication_mpi(element_type **matrix1,
                                           element_type **matrix2,
                                           element_type **result,
                                           size_t matrix_dim,
                                           int block_matrix_dim) {
  return 0;
}
